// Normal function 

let log = function(message) {
    console.log(message);
}

// arrow function 
// This helps write cleaner code and help organise your workflow better, it's also an added advantage if you have knowledge of it.

let doLog = (message) => {
    console.log(message)
}

// Or write it more cleaner 

let doLogTwo = (message) => console.log(message); 